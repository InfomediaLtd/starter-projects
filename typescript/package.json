{
  "scripts": {
    "test": "rm -rf ./test && mkdir ./test && cd ./test && cat ../index.js | node && npm install",
    "deploy": "rm -rf ./deploy && mkdir deploy && cp index.* ./deploy && surge -p ./deploy -d typescript.surge.sh"
  }
}
